apiVersion: apps/v1
kind: Deployment
metadata:
  name: engine-y-deployment
spec:
  selector:
    matchLabels:
      app: engine-y
  replicas: 2
  template:
    metadata:
      labels:
        app: engine-y
    spec:
      containers:
        - name: nginx
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 80
